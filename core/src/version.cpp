// ---------------------------------------------------------------------------
// xpscenery — version constants (separate TU so that rebuilds are cheap)
// ---------------------------------------------------------------------------

extern "C" const char* xps_version_tag(void) {
    return "0.1.0-alpha.0";
}
